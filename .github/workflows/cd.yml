on:
  push:
    branches: [main]
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23.0"

      - name: Build the app
        run: ./scripts/buildprod.sh

      - name: 'Auth to GCP Service Account'
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIAL }}'

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v3'

      - name: 'Use gcloud CLI'
        run: 'gcloud info'

      - name: 'Docker build and Publish to Google Artifacts'
        run: 'gcloud builds submit --tag us-central1-docker.pkg.dev/notely-474000/notely-ar-repo/notely:latest .'
      - name: Deploy to Cloud Run
        run: gcloud run deploy notely --image us-central1-docker.pkg.dev/notely-474000/notely-ar-repo/notely:latest --region us-central1 --allow-unauthenticated --project notely-474000 --max-instances=4